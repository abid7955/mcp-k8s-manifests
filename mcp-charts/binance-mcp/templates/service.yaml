apiVersion: v1
kind: Service
metadata:
  name: {{ include "binance-mcp.fullname" . }}
  labels:
    {{- include "binance-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "binance-mcp.selectorLabels" . | nindent 4 }}
